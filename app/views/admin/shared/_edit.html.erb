<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>

<% if current_user.admin? %>
  <h3>Merge articles</h3>

  <%= form_tag(:id => params[:id], :enctype => "multipart/form-data") do %>
    Article ID  <%= text_field_tag :merge_with %> </br>
    <%= submit_tag "Merge With This Article", class: 'btn' %>
 <%end%>

<% end %>
